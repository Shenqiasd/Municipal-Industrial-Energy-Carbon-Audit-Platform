spring:
  datasource:
    url: jdbc:mysql://localhost:3306/energy_audit?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: root

jwt:
  secret: energy-audit-dev-secret-key-for-development-only-min-256-bits
  expiration: 86400000

logging:
  level:
    com.energy.audit: debug
    org.mybatis: debug
