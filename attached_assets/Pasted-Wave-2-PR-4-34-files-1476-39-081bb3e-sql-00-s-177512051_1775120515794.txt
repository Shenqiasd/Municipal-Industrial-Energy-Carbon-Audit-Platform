Wave 2 PR #4 — 综合代码审核报告（人工逐文件审核）
审核范围: 34 files, +1476 −39 | 提交: 081bb3e
审核方法: 逐文件阅读全部代码，对照 sql/00-schema.sql 生产表结构验证 Entity/Mapper/H2 对齐

总结
级别	数量	说明
🔴 严重 (Critical)	1	阻塞合并，生产环境 SQL 错误
🟡 中等 (Medium)	3	建议合并前修复
🔵 非阻塞 (Non-blocking)	3	可后续迭代修复
建议：修复 1 个严重问题后可合并。

🔴 严重问题 (Critical — 阻塞合并)
C-1: ent_enterprise_setting — Entity/Mapper/H2 包含 remark 列，但生产 schema 没有
文件:

EntEnterpriseSetting.java — private String remark;
EntEnterpriseSettingMapper.xml — resultMap <result column="remark" .../> + upsert SQL INSERT/UPDATE 都引用 remark
schema-h2.sql — remark VARCHAR(512)
sql/00-schema.sql — ent_enterprise_setting 表不包含 remark 列
后果: 在生产 MySQL 执行 upsert SQL 时抛出 Unknown column 'remark' in 'field list'，企业设置保存功能完全不可用。

这与 PR #2 中 tpl_template.remark 问题完全相同。建议建立 schema 变更清单，确保 Entity 每个字段都有生产 DDL 对应。

修复（二选一）：

从 Entity、Mapper XML、H2 schema 中移除 remark
在 sql/00-schema.sql 生产表中添加 remark VARCHAR(512) DEFAULT NULL COMMENT '备注'
🟡 中等问题 (Medium — 建议合并前修复)
M-1: BsUnitEnergyMapper.xml — 两个软删除方法缺少 update_by 审计追踪
<update id="deleteByUnitIdAndEnergyId">
    UPDATE bs_unit_energy
    SET deleted = 1, update_time = NOW()   <!-- 缺少 update_by = #{updateBy} -->
    WHERE unit_id = #{unitId} AND energy_id = #{energyId} AND deleted = 0
</update>

<update id="deleteAllByUnitId">
    UPDATE bs_unit_energy
    SET deleted = 1, update_time = NOW()   <!-- 缺少 update_by = #{updateBy} -->
    WHERE unit_id = #{unitId} AND deleted = 0
</update>
后果: 无法追溯谁删除了单元-能源关联。这是 PR #1 审核中已修复过的审计追踪问题，在 Wave 2 新代码中再次出现。

修复: SQL 添加 update_by = #{updateBy}，Java 接口添加 @Param("updateBy")，Service 层传入 SecurityUtils.getCurrentUsername()。

M-2: UnitSettingServiceImpl.delete() 未级联软删除 bs_unit_energy 关联
@Override
public void delete(Long id) {
    getById(id);
    unitMapper.deleteById(id, SecurityUtils.getCurrentUsername());
    // 缺少: unitEnergyMapper.deleteAllByUnitId(id);
}
后果: 删除单元后，关联的 bs_unit_energy 记录仍为 active（deleted=0），getUnitEnergies(deletedUnitId) 仍返回数据。

对比: SysDictServiceImpl.deleteType() 正确实现了级联删除。

M-3: UnitSettingServiceImpl.addUnitEnergy() 未校验 unit/energy 是否存在
public void addUnitEnergy(Long unitId, Long energyId) {
    // 缺少: getById(unitId) / energyService.getById(energyId) 验证存在性
    BsUnitEnergy ue = new BsUnitEnergy();
    ue.setUnitId(unitId);
    ue.setEnergyId(energyId);
    unitEnergyMapper.insert(ue);
}
后果: 软删除模式无物理外键约束，传入不存在的 ID 会创建孤儿关联记录。

🔵 非阻塞建议 (Non-blocking)
#	说明	文件
N-1	importFromCatalog() 逐条 INSERT（循环 N 次 DB），大批量导入性能差，建议 batch insert	EnergySettingServiceImpl.java
N-2	提交包含 2 个非代码文件 attached_assets/image_*.png，应从仓库移除	attached_assets/
N-3	ON DUPLICATE KEY UPDATE 使用 VALUES() 函数，MySQL 8.0.20+ 已标记 deprecated，未来建议迁移 new_row 别名语法	EntEnterpriseSettingMapper.xml
已验证通过 ✓
模块	验证内容	状态
CmEmissionFactor Entity ↔ 生产 Schema	全部列名/类型一致	✓
BsEnergyCatalog Entity ↔ 生产 Schema	全部列名/类型一致	✓
BsUnitEnergy Entity ↔ 生产 Schema	全部列名/类型一致	✓
4 个 Mapper XML resultMap + CRUD	列名映射正确	✓
H2 ↔ 生产 Schema	3 张新表完全一致（ent_enterprise_setting 除外见 C-1）	✓
deleteById 审计	CmEmissionFactor / BsEnergyCatalog 包含 update_by + update_time	✓
@Cacheable + @CacheEvict	EnergySettingServiceImpl 缓存使用正确	✓
@transactional	importFromCatalog / addUnitEnergy 正确使用事务	✓
分页	所有 list 端点统一 PageHelper + PageResult	✓
Controller API 设计	RESTful 风格一致	✓
Unit/Product Controller 升级	TODO 桩代码 → 完整实现	✓
SecurityUtils enterpriseId 注入	企业端 Service 统一注入	✓
整体评价：Wave 2 代码质量延续 Wave 1 高水准，新增模块架构一致、CRUD 模式规范。importFromCatalog 从全局目录导入到企业能源列表的核心业务设计合理。修复 C-1 后即可合并。